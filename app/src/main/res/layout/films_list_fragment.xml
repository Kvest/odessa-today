<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <TextView
        android:id="@+id/date"
        android:layout_width="match_parent"
        android:layout_height="@dimen/films_date_label_height"
        android:paddingLeft="@dimen/films_date_label_horizontal_padding"
        android:paddingRight="@dimen/films_date_label_horizontal_padding"
        android:gravity="center_vertical|left"
        android:background="@color/films_date_bg"
        android:textColor="@color/films_date_text_color"
        android:textSize="@dimen/films_date_text_size"
        android:singleLine="true"
        android:ellipsize="end"/>
    <android.support.v4.widget.SwipeRefreshLayout
        android:id="@+id/refresh_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <ListView
            android:id="@+id/films_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@color/films_list_divider_color"
            android:dividerHeight="@dimen/films_list_divider_height"
            android:listSelector="@drawable/application_list_item_selector" />
    </android.support.v4.widget.SwipeRefreshLayout>
</LinearLayout>
