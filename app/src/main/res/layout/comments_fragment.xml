<?xml version="1.0" encoding="utf-8"?>

<com.sothree.slidinguppanel.SlidingUpPanelLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:sothree="http://schemas.android.com/apk/res-auto"
    android:id="@+id/sliding_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="bottom"
    sothree:umanoPanelHeight="@dimen/add_comment_panel_header_height">
    <ListView
        android:id="@+id/comments_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:listSelector="@android:color/transparent"
        android:cacheColorHint="@android:color/transparent"
        android:divider="?attr/CommentsListDividerColor"
        android:dividerHeight="@dimen/comments_list_divider_height" />
    <LinearLayout
        android:id="@+id/add_comment_panel"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/application_green"
        android:layout_gravity="bottom">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="@dimen/add_comment_panel_header_height"
            android:orientation="horizontal"
            android:weightSum="1">
            <ImageView
                android:id="@+id/open_panel_icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_open_comment_panel"
                android:scaleType="center"
                android:background="@null"
                android:minWidth="@dimen/add_comment_button_min_size"
                android:minHeight="@dimen/add_comment_button_min_size"
                android:layout_gravity="center_vertical"/>
            <TextView
                android:layout_width="0dip"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_gravity="center_vertical"
                android:gravity="center"
                android:textSize="@dimen/write_comment_text_size"
                android:textColor="@color/write_comment_text_color"
                android:text="@string/write_comment" />
            <ImageButton
                android:id="@+id/send"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="invisible"
                android:src="@drawable/ic_send_comment"
                android:background="@null"
                android:minWidth="@dimen/add_comment_button_min_size"
                android:minHeight="@dimen/add_comment_button_min_size"
                android:layout_gravity="center_vertical"/>
        </LinearLayout>
        <EditText
            android:id="@+id/comment_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="@dimen/add_comment_edit_text_padding"
            android:background="@color/add_comment_edit_text_bg"
            android:hint="@string/name"
            android:singleLine="true"
            android:maxLength="100" />
        <EditText
            android:id="@+id/comment_text"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="@dimen/add_comment_text_top_margin"
            android:padding="@dimen/add_comment_edit_text_padding"
            android:gravity="top|left"
            android:background="@color/add_comment_edit_text_bg"
            android:maxLength="1000"
            android:hint="@string/comment_text"
            android:inputType="textMultiLine"
            android:scrollbars="vertical"/>
    </LinearLayout>
</com.sothree.slidinguppanel.SlidingUpPanelLayout>