<?xml version="1.0" encoding="utf-8"?>

<android.support.v4.widget.SwipeRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/refresh_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <ListView
        android:id="@+id/cinemas_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/cinemas_list_bg_color"
        android:divider="@color/cinemas_list_divider_color"
        android:dividerHeight="@dimen/cinemas_list_divider_height"
        android:listSelector="@drawable/application_list_item_selector" />
</android.support.v4.widget.SwipeRefreshLayout>