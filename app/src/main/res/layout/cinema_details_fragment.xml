<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent">
    <ListView
        android:id="@+id/cinema_details_list"
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:paddingBottom="@dimen/cinema_details_padding"
        android:background="?attr/CinemaDetailsBgColor"
        android:listSelector="@drawable/application_list_item_selector"
        android:divider="@null"/>
    <FrameLayout
        android:id="@+id/calendar_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</FrameLayout>

